<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</head>

<body class="bg-light  ">
   
        <div class="d-flex flex-column justify-content-between vh-100 ">
            <%- include("../partials/header") %>
            <form action="/login" method="POST" class="flex-grow-2 border border-dark rounded p-4 bg-white shadow m-auto"
                style="width: 100%; max-width: 400px;">
                <fieldset>
                    <legend class="mb-4 d-flex align-items-center justify-content-center">
                        Login
                        <img class="ms-2" src="/images/kitty.png" alt="kitty" style="width: 40px; height: 40px;">
                    </legend>

                    <div class="mb-3">
                        <label for="email" class="form-label">Email:</label>
                        <input type="text" id="email" name="email" class="form-control">
                    </div>

                    <div class="mb-3">
                        <label for="password" class="form-label">Password:</label>
                        <input type="password" id="password" name="password" class="form-control">
                    </div>

                    <button type="submit" class="btn btn-success w-100">Login</button>

                    <% if (error) { %>
                        <div class="mt-3 alert alert-danger" role="alert">
                            <%= error %>
                        </div>
                        <% } %>

                </fieldset>
            </form>
            <%- include("../partials/footer") %>
        </div>
        
</body>

</html>